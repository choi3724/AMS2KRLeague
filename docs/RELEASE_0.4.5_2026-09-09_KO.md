# 0.4.5 게시 전 검증 보고서
2026-09-09. 이 문서는 커밋·태그를 만들기 직전의 검증 스냅샷이다. 사용자가 0.4.5 커밋 및 정식 Latest 게시를 명시적으로 요청했다.

## 기준선
- main/origin/main: 41857051c20f2c992304c11ae53c78e774f887a1, 공개 v0.4.4.
- 주행 계기판 작업 시작 전 Client 113/113, Activity 102/102. 직전 테스트 후보는 Client 117/117, Activity 104/104.
- 기존 읽기 전용 SHM, Compact 원본·ID·해시, 모드/권한 검사, 재시도·중복 방지, 기존 패널 배치를 보호했다.

## 요구사항
- REQ-DRIVE-01~08 DONE: 자동 재실행, 독립 텔레메트리·속도·기어, 색상/글꼴 저장, A/B/C/H, 사용자 아이콘, 한글패치 Pretendard Medium 내장.
- REQ-DRIVE-09 DONE: 제목 '텔레메트리'만 표시. 화면 선에만 100ms 시간 기준 평활화와 이차 곡선 연결. 막대와 수집/저장/전송 값은 원본 유지.
- REQ-SEND-01 DONE: 동일 캡처 세션의 연습·예선·레이스를 전체 레이스 종료까지 로컬 보관. 안정된 결과와 기존 종료 ledger의 durable 완료를 확인한 뒤 대기열을 해제한다.
  - 한 참가자의 완주만으로 전송하지 않는다. 전체 드라이버 종료가 안정된 결과를 사용한다.
  - 세이프티카의 위치 0·주행 상태는 원본에 보존하고 드라이버의 종료·재출발 판정에서 제외한다.
  - 종료 결과 화면이 반복되어도 새 캡처를 만들지 않는다. 다음 연습/예선 또는 재출발에서는 새 캡처를 시작한다.
  - 늦게 참가한 사용자의 실제 시작을 유지한다. 테스트에서 시작 0초/40초, 종료 72초/72초 확인. PC의 관측 UTC를 임의로 보정하거나 미관측 구간을 생성하지 않는다.
  - 이미 저장된 Witness와 종료 ledger에서 전송 허용 상태를 복원한다. 별도 업로드 프로토콜·중복 manifest를 만들지 않았다.
  - 완료 근거가 없는 다른 경기/손상된 ledger는 보류하고 원본을 유지한다.
- REQ-WEB-01 DEFERRED: 과거 웹사이트의 종료 무결성/사고 조회 경고는 사용자 요청에 따라 별도 웹 분석 후 수정.
- REQ-RELEASE-045 READY: 0.4.5 버전 정합성, 배포 빌드/감사, 설치 검사 완료. 사용자 승인 범위에 따라 커밋·태그·푸시·Latest 게시를 이어서 수행한다.

## 검증
- scripts/build-release.ps1: Release 경고 0, 오류 0, **Client 117/117**, **Activity 104/104**.
- scripts/verify.sh와 동일한 .NET 두 스택을 릴리스 게이트에서 검사했고, ApplyUpdate.ps1 구문 검사도 통과했다.
- 그래프 전용 WPF 검증: 1/1. 원본 수직 변화량 113.8 → 화면 곡선 14.6, 0~100% 범위 유지, 미확인 구간 단절.
- 동일 입력점을 350ms 동안 x=323.9 → 309.8로 이동시킨 프레임 계측. WPF 미리보기 30프레임.
- 새 전송 테스트: 종료 전 전송 0건, 늦은 참가자 공통 종료, 지속 주행 중인 세이프티카 원본 보존, 반복 결과 억제, 완료 증거 복원, 불완전한 별도 경기 전송 차단.
- 자기 완결형 win-x64 publish, ZIP/설치 파일 감사 PASS (publish 467개 파일, 금지 자료 0건).
- 실행/설치 파일의 ICO 7개 프레임이 원본과 각각 일치.
- 격리 휴대용 폴더에 실제 설치: 종료 코드 0, 실행 파일 버전 0.4.5, DLL 해시 일치, 폰트 라이선스 포함.
- 실게임 조작, 사용자 설치 변경, 운영 서버/DB 변경은 하지 않았다.

증거:
- work/release-0.4.5-final-build.log
- work/graph-0.4.5-final.log
- work/race-batch-safetycar.log
- work/validation-0.4.5/after/
- work/validation-0.4.5/icon-proof/
- work/install-0.4.5-proof.log

## 전송량과 의미
- HTTP 본문/Compact 스키마/기존 청크 크기는 유지한다. 추가 네트워크 필드 0개, 별도 종료 알림 요청 0개.
- 종료 후 기존 청크 여러 개를 대기열에서 순서대로 전송한다. 서버의 원자적 단일 요청 업로드로 바꾼 것이 아니다.
- 결과 종료 이후 불필요한 추가 캡처를 멈춘다. 손실 ledger가 PARTIAL이면 PARTIAL을 유지하며 완전한 기록으로 위장하지 않는다.
- 전체 종료를 관측하지 못하거나 저장 완료 근거가 없으면 보관한다. 기존 싱글·미확인·권한 부족 보류도 유지한다.

## 요청하지 않은 변경
- 범위 밖 기능 추가 0건.
- 별도 작업에서 들어온 예선 종료 상태 혼입 및 빈 트랙 캡처 방지 변경을 보존하고 이번 전체 테스트에 포함했다.
- 세이프티카 관련 보완은 새 종료 후 전송 기능의 마감이 막히는 문제를 해결하기 위한 변경이며 원본 행을 삭제하지 않았다.

## 게시 계획
- 태그: v0.4.5 / 정식 Latest (isPrerelease=false).
- 설치 파일, win-x64 ZIP, SHA256SUMS, release-manifest 4개 자산.
- 게시 후 확인: https://github.com/choi3724/AMS2KRLeague/releases/tag/v0.4.5
