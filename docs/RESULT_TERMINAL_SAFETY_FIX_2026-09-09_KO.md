# 종료 상태·세이프티 카 판정 수정 인수인계

2026-09-09. 웹 결과 감사에서 재현한 두 오버레이 결함에 대한 사용자 승인 후 수정이다.

## 변경

- `HostRecorderEngine.cs`: 공통 종료 판정을 witness에서도 재사용한다. 이름과 명시적인 FIN/RET/DNF/DSQ 상태가 있는 비활성 참가자는 최종 결과·증거·상태 전이에 보존한다. 출발 그리드는 활성 참가자만 포함한다. 사라진 참가자의 종료를 추정하지 않고 거짓 재접속 전이를 만들지 않는다.
- `SessionWitnessCaptureEngine.cs`: 세이프티 카가 Racing으로 남아도 인간 전체 종료 판정을 막지 않는다. 세이프티 카 원본은 유지한다. 비활성 명시적 종료 상태를 단순 실종으로 덮지 않는다.
- `SessionWitnessTests.cs`: SC 포함 완전 범위 판정, 미종료 인간/SC 단독의 오판 방지, 비활성 종료 참가자와 wire 보존, 실제 실종 및 출발 그리드 제외 회귀 검사를 추가했다.

## 검증

- Release 빌드: 오류 0, 경고 0.
- Activity: 107/107, Client: 117/117.
- 웹 작업 폴더의 독립 오버레이 원인 재현 검사 2개: 수정 후 통과.
- SDK: `C:/Users/User/Documents/Codex/2026-08-25/files-pasted-by-the-user-2026/work/dotnet8/dotnet.exe` (.NET 8.0.424). 이 환경에서는 저장소 `work/dotnet8` 경로가 없었다.

## 하지 않은 작업과 다음 단계

이 작업에서는 커밋·태그·푸시·릴리스와 실게임 실행을 하지 않았다. wire 형식, 전송 정책, 기존 보관 원본을 변경하지 않았다. 수정 후 별도 버전 0.5.1 갱신 변경들이 같은 작업 트리에 나타났으며 해당 파일은 건드리지 않았다. 위 테스트 수치는 버전 갱신 작업 전 이번 세 파일에 대해 실행한 결과다.

인간 종료 판정이 올바르다는 것과 전체 스트림 수신 무결성은 별개다. 웹의 종료 무결성 미확인 안내를 제거하려고 과거 자료를 임의로 완전 기록으로 바꾸면 안 된다. 새 일괄 전송의 manifest/finalize 계약과 실제 서버 수신 종단 검증은 웹 명세의 후속 작업이다. 실제 멀티플레이 종료 업로드 검증은 다음 사용자 경기 자료로 수행해야 한다.
