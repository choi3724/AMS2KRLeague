<UserControl x:Class="AMS2LeagueClient.Presentation.MultiplayerWaitingOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="{DynamicResource HudFontFamily}"
             IsHitTestVisible="False">
    <Border CornerRadius="12" Background="#F00A111C" BorderBrush="#8257D5FF" BorderThickness="1.5" Padding="18,14">
        <StackPanel>
            <TextBlock Text="{Binding Title}" Foreground="#57D5FF" FontWeight="Bold" FontSize="17" />
            <TextBlock Text="{Binding SessionLabel}" Foreground="White" FontWeight="SemiBold" FontSize="21" Margin="0,7,0,0" />
            <Grid Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{Binding ParticipantCountText}" Foreground="#91A5B8" FontSize="12" />
                    <TextBlock Text="{Binding RemainingLabel}" Foreground="#91A5B8" FontSize="11" Margin="0,3,0,0" />
                </StackPanel>
                <TextBlock Grid.Column="1" Text="{Binding RemainingValue}" Foreground="#FFCA7A" FontWeight="Bold" FontSize="16" Style="{StaticResource HudNumericText}" VerticalAlignment="Bottom" />
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
